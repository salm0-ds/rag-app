## Multi-tenancy Rag Application

subtitle

description
The main showcase of this project is the backend. The frontend is completely AI-generated 
This is a 
Users create their own login, then create a folder which contains documents within that folder to be queried on search, multiple chats can be created in one folder. Users can create, rename and delete folders, documents and chats. Many file types such as pdf, txt, docx are supported. Documents can be queried 

video
app workflow

The app will be deployed AWS with the following architecture
system architecture

how to install
